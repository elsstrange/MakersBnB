<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="style.css">
    <title>MakersBNB</title>
  </head>
  <body>
    <%= erb :'partials/header' %>
    <h1>Feel at home, anywhere</h1>
    <% if @user %>
      <div class="centred">
        <form action="/spaces/new" method="GET">
          <button class="preferred-action" type="submit">List a Space</button>
        </form>
      </div>
    <% end %>
    <section class="wrapped-list">
      <% @spaces.each do |space| %>
        <section class="list-item" id="space<%= space.id %>">
          <a class="more-info" href="/spaces/<%= space.id %>">></a>
          <h2><%= space.name %></h2>
          <p><%= space.short_description(75) %>...</p>
          <p>£<%= space.price %> per night</p>
        </section>
        <div class="spacer"></div>
      <% end %>
    <section>
  </body>
</html>