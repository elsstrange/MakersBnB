<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <%= erb :'partials/header' %>
    <h1>Feel at home, anywhere</h1>
    <% if @user %>
      <form action="/spaces/new" method="GET">
        <button type="submit">List a Space</button>
      </form>
    <% end %>
    <% @spaces.each do |space| %>
      <article class="space" id="space<%= space.id %>">
        <h2><%= space.name %></h2>
        <p><%= space.short_description(50) %>...</p>
        <p>£<%= space.price %> per night</p>
        <form action="/spaces/<%= space.id %>" method="GET">
          <button type="submit" name="more-info">>></button>
        </form>
      </article>
    <% end %>
  </body>
</html>